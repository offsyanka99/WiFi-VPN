<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:id="@+id/widgetTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/app_name"
        android:textColor="@color/md_theme_on_surface"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="home" />

    <TextView
        android:id="@+id/widgetMonitoring"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/status_monitoring_off"
        android:textColor="@color/status_off"
        android:textSize="13sp"
        tools:text="Monitoring: on" />

    <TextView
        android:id="@+id/widgetWifi"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/status_wifi_disconnected"
        android:textColor="@color/status_warn"
        android:textSize="13sp"
        tools:text="Wi‑Fi: trusted (Home)" />

    <TextView
        android:id="@+id/widgetVpn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/status_vpn_off"
        android:textColor="@color/status_off"
        android:textSize="13sp"
        tools:text="VPN: on" />

    <TextView
        android:id="@+id/widgetToggle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@drawable/widget_button_background"
        android:gravity="center"
        android:paddingHorizontal="8dp"
        android:paddingVertical="8dp"
        android:text="@string/btn_start_monitoring"
        android:textColor="@color/md_theme_on_primary_container"
        android:textSize="12sp"
        android:textStyle="bold" />
</LinearLayout>
